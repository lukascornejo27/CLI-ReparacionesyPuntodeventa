import { BuscarCliPipe } from './buscar-cli.pipe';

describe('BuscarCliPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarCliPipe();
    expect(pipe).toBeTruthy();
  });
});
