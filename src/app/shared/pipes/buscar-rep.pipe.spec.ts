import { BuscarRepPipe } from './buscar-rep.pipe';

describe('BuscarRepPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarRepPipe();
    expect(pipe).toBeTruthy();
  });
});
